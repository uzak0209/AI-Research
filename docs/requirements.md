# 要件

何を・なぜ。手段は [ADR](adr/)。

## 問題・ユーザー

手作業: トレンド調査 / 重複確認 / 有用論文の発見 / **次のテーマ候補** / 参考文献の整理。

- 論文を書く研究者。未公開データ・原稿を手元に持つ
- 分野非依存。配布する（`C-02`）
- **特定の参考文献マネージャ（Zotero 等）への依存は前提にしない**

## 成功条件（初版）

1. アプリ未起動でも日次で新着候補が溜まる
2. 起動すると、**ローカル推論で精査された「有効な論文」**が読める（重複・活用可能性つき）
3. **アプリ内で参考文献を管理でき、原稿から `\cite{}` できる状態まで自動で届く**
4. 執筆中原稿の主張を検査できる
5. 外部 LLM を統制下で呼べる（**利用自体が目的**。ゲートウェイは ADR-0002）
6. 新着から次のテーマ候補を提示できる
7. **GUI と同じローカルデータに CLI から触れられる**

やらない: 全分野完全網羅 / クラウド完結の研究環境 / 外部 LLM ゼロを既定 /
**初版で特定マネージャの必須連携** / CLI だけで GUI を置き換えること

## 機能 (FR)

| ID | 内容 | 成功 | ADR |
|---|---|---|---|
| FR-01 | 日次収集（起動非依存）。失敗と 0 件を区別 | 1 | 0001 |
| FR-02 | **関連度順の提示**（読む順を決められる。初版は有効／除外の断定をしない） | 2 | 0001 |
| FR-03 | 手元研究データの検索（`C-01`） | 2,4 | 0001 |
| FR-04 | 原稿ファクトチェック（手動必須。定期は未決） | 4 | 0003 |
| FR-05 | **アプリ内参考文献ライブラリ**（追加・閲覧・整理。日次候補から保存） | 3 | 0003 |
| FR-06 | プロジェクト／分野の切り替え | — | 0001 |
| FR-07 | 原稿取り込み（md/tex/typ、監視＋手動） | 4 | 0003 |
| FR-08 | 欠損を成功と偽らない（「0 件」と「取得失敗」を区別） | 1 | 0001 |
| FR-09 | テーマ候補（文脈を踏まえテーマ粒度） | 6 | 0001 |
| FR-10 | 外部 LLM の統制利用 | 5 | 0002 |
| FR-11 | **CLI**（GUI と同一のローカルストア。少なくともライブラリ操作） | 7 | 0001,0003 |
| FR-12 | **引用ファイルへの自動書き出し**（`.bib` / Hayagriva。ライブラリと同期、既存に追記） | 3 | 0003 |
| FR-13 | **ローカル推論による関連度付け**（収集分を手元で採点。採点目的で外部へ送らない） | 2 | 0001 |
| FR-14 | **PDF 添付・アプリ内閲覧・注釈／ノート**（外部ビューア不要） | 3 | 0003 |

### FR-05 / FR-11 の受け入れ（要約）

- 書誌（タイトル・著者・年・DOI 等）をローカルに保持・検索できる
- コレクション／タグ相当で整理できる
- 日次レポートや判定結果から「ライブラリに入れる」ができる
- CLI は GUI と**同じライブラリ**を読む／書く（二重管理しない）
- Electron 未起動でも CLI だけでライブラリ操作できる
- ライブラリに入れた文献が、書き出し先設定済みなら `\cite{}` できる状態になる（FR-12）
- 既存の引用ファイルに手で書いた項目が、書き出しによって壊れない（`C-08`）
- 他マネージャからの取込・双方向同期は初版必須ではない → 未決／候補

### FR-13 / FR-14 の受け入れ（要約）

- 収集済み候補が**関連度順に並ぶ**。何件見れば取りこぼさないかを実測値で言える
- 各候補に「**自分のどの提案手法に最も近いか**」が併記される。最近傍という事実であって判定ではない
- 採点はネットワーク不通でも実行できる（`NFR-02`）
- 採点できていない候補は「未採点」と示す。順位を捏造しない（`C-07`）
- 候補は捨てられない。順位が下でも一覧から辿れる
- 採点済みの件から順に一覧へ出る。途中で終了しても済んだ分は残る
- 採点の実行中もライブラリと PDF ビューアを操作できる（`NFR-06`）
- ライブラリの文献に PDF を添付し、アプリ内で開いてハイライト・注釈・ノートを付けられる
- 注釈・ノートを付けても、添付した PDF 原本のバイト列は変わらない（`C-08`）

**初版で満たさないこと**（実測の結果。詳細は
[prototypes/judge-bench/FINDINGS.md](../prototypes/judge-bench/FINDINGS.md)）:

- **「有効／除外」の断定をしない。** 実データで類似度の閾値が引けなかった
  （有効の最小 0.7237 < 除外の最大 0.7469）
- **判定の理由を出さない。** 理由は生成 LLM が要る。小型モデルは理由を捏造したため見送った
- **「重複（競合）」と「活用」を区別しない。** 類似度はこの 2 つを区別しない。
  どちらも高く出るため **上位には競合論文が混ざる**。利用者が読んで判断する

## 制約 (C) / 非機能 (NFR)

| ID | 内容 | ADR |
|---|---|---|
| C-01 | 未公開の恒久外部インデックス禁止。埋め込み≠プロンプト | 0001 |
| C-02 | 配布前提 | 0001,0002 |
| C-03 | 未公開を扱う機能は完全オフ可 | 0002,0003 |
| C-04 | 外部推論は分類・経路固定・同意。範囲を偽らない | 0002,0003 |
| C-05 | 分野知識をコアにハードコードしない | 0001 |
| C-06 | 開発者の有料 API キーをクライアントに置かない | 0002 |
| C-07 | 欠損≠成功表示 | 0001 |
| C-08 | **ユーザーのファイルを壊さない。**書き換えは自分の管理範囲のみ | 0003 |
| C-09 | **関連度の採点はローカル推論で完結。**採点目的で論文・プロフィール・原稿を外部 LLM に送らない | 0001,0002 |
| NFR-01 | 日次収集は PC 起動に依存しない | 0001 |
| NFR-02 | 手元検索・ライブラリ・**関連度の採点**はオフライン継続可 | 0001,0003 |
| NFR-03 | 段階縮退（C3 は秘匿優先可） | 0001,0002 |
| NFR-04 | 利用者単位の利用上限 | 0002 |
| NFR-05 | 実行時必須ランタイムを増やしすぎない（**ローカル LLM も既存の推論基盤に載せる**） | 0001 |
| NFR-06 | **採点中も UI をブロックしない**（ライブラリ・PDF 閲覧は継続可） | 0001,0003 |

スコープ外の例: Overleaf 専用橋、埋め込み API で personal を索引化、キー同梱、
**特定マネージャの必須前提**、**採点のために論文本文や原稿を外部 LLM へ送ること**、
**ローカル推論のために別プロセス／別ランタイムの導入をユーザーに求めること**、
**初版での有効／除外の断定と理由生成**。

## 未決 / 候補

未決: FC 定期、レポート UX、FC 結果保存、テーマ UI、
CSL スタイル対応、他マネージャからの移行、CLI のコマンド粒度、
書き出し先の指定 UX（原稿から自動推測するか、明示設定のみか）、
**関連度の表示 UX（何件を既定で見せるか）**、**本文チャンクを採点に使うか**、
**有効／除外の断定と理由生成を次版で入れるか**、
**PDF 本文の自動取得（オープンアクセス限定）**。

候補: トリアージ、プロフィール学習、ウォッチリスト、週次ダイジェスト、
他マネージャとの双方向同期（任意アドオン扱い）。
